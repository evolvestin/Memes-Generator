# Telegram Meme Aggregator Bot

![Status](https://img.shields.io/badge/Status-Discontinued-red)
![Python](https://img.shields.io/badge/Python-3.x-blue)
![Library](https://img.shields.io/badge/Telebot-Async-yellow)

> [!CAUTION]
> **ПРОЕКТ ЗАКРЫТ И НЕ ПОДДЕРЖИВАЕТСЯ**
>
> Обслуживание и разработка данного программного обеспечения были полностью прекращены в **2023 году**.
>
> **Причина остановки:** Эксплуатация алгоритмического подбора контента продемонстрировала неудовлетворительное качество публикуемых материалов (низкая релевантность юмора, пропуск рекламных интеграций, ошибки парсинга). В связи с этим было принято решение отказаться от автоматизации в пользу **ручного ведения канала** для обеспечения контроля качества.
>
> Код предоставляется исключительно в ознакомительных целях "как есть" (as is) и может требовать существенной доработки для запуска в современных условиях.

## Описание проекта

Данный скрипт представляет собой автоматизированную систему агрегации развлекательного контента (мемов) из списка источников в Telegram. Бот осуществляет мониторинг указанных каналов, анализирует популярность публикаций и пересылает наиболее востребованный контент в канал-фильтр для премодерации.

Целевой канал проекта: https://t.me/biochememes (ныне ведется вручную).

## Функциональные возможности

Архитектура бота построена на взаимодействии с Google Sheets и Telegram API, выполняя следующий цикл задач:

1.  **Управление источниками:** Список каналов-доноров хранится и управляется через Google Таблицу.
2.  **Алгоритмический отбор:** Скрипт анализирует последние публикации в каналах-донорах. Популярность поста определяется путем сравнения количества просмотров конкретной записи с медианным значением просмотров в данном канале.
3.  **Фильтрация контента:**
    *   Попытка автоматического обнаружения и исключения рекламных постов (фильтрация по ключевым словам, ссылкам на другие каналы).
    *   Исключение дубликатов ссылок, которые уже были обработаны.
4.  **Процесс публикации:**
    *   Отобранный контент отправляется в закрытый канал-фильтр.
    *   Администратор имеет возможность одобрить (👍) или отклонить (👎) публикацию через Inline-клавиатуру.
    *   Одобренные посты автоматически публикуются в основном канале.

## Технический стек

*   **Язык:** Python
*   **Библиотеки:**
    *   `pyTelegramBotAPI (telebot)` — взаимодействие с Telegram API.
    *   `gspread` — работа с Google Sheets API.
    *   `BeautifulSoup4` — парсинг HTML-представлений постов (для получения метрик просмотров и дат, если API недоступно или ограничено).
    *   `requests` — загрузка медиафайлов.

## Известные ограничения (Legacy)

На момент прекращения поддержки были зафиксированы следующие технические недочеты:

*   **Парсинг видео:** Некорректная обработка некоторых форматов видеофайлов и GIF-анимаций при скачивании.
*   **Рекламный фильтр:** Недостаточная эффективность Regex-фильтров, приводящая к пропуску нативной рекламы и постов с интеграциями.
*   **Кнопки и медиа-группы:** Проблемы с отображением Inline-кнопок под сообщениями, содержащими группу медиафайлов (альбомы).

## История изменений

*   **28.11.2022** — Рефакторинг кода. Модуль `e-objects` версии 1.2.0 интегрирован в `functions.py`. Добавлена точка входа `start()`.
*   **16.01.2021** — Выявлены проблемы со скачиванием медиафайлов через прямые ссылки Telegram.
*   **26.12.2020** — Внедрение алгоритмов фильтрации рекламных объявлений. Удаление упоминаний авторов (forwarded from) для "чистоты" поста. Временное отключение кнопок для альбомов.
*   **12.11.2020** — Обновление вспомогательных библиотек.
